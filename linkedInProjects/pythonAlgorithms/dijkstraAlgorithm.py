def main():
    unvisited = [B, C, D, E]
    visited = [A]
    current_vertex = A
    A = {"B": 2, "B": 3}
    
    while unvisited:
        for x in range(len(current_vertex)):
            smallestDist = 0
            if current_vertex[x]:
                break
    
